#!/usr/bin/env ruby
require 'highline/import'

Dir["./app/**/*.rb"].each { |f| require f }
Dir["./lib/*.rb"].each { |f| require f }

if ARGV.length == 1 and ARGV[0] == "new"
  CigarsController.new.new_cigar
elsif ARGV.length == 1 and ARGV[0] == "edit"
  #future stuff
elsif ARGV.length == 1 and ARGV[0] == "edit"
  #future stuff
else
  puts "[Help] Run as: ./ash_tray (new, edit or list)"
end
